# PROJECT: クマ出没予測 Web アプリ

> 状態管理ファイル。マイルストーン進捗と次アクションをここで追う。

## 現在地

- *フェーズ*: M3（翌週出没確率予測 + リスクランキング + 地図ダッシュボード）コード完成・実データ検証済み
- *最終更新*: 2026-06-05

## マイルストーン

| MS | 内容 | 状態 |
|----|------|------|
| M1 | CKAN 取得 → クレンジング → SQLite → Leaflet 出没点プロット | ✅ 完成・実データ検証済み |
| M2 | 1km メッシュ集計 → 週次パネル化 → リークフリー特徴量 + 時系列ベースライン | ✅ 完成・実データ検証済み |
| M3 | GBDT → 翌週確率 → 時系列分割評価 → リスクランキング + 地図 | ✅ コード完成・実データ検証済み |
| M4 | 外部特徴量（堅果豊凶・土地被覆・標高・気象）で精度改善 | ⬜ 任意 |

## M3 実データ検証結果（2026-06-05, live CKAN）

- モデル: sklearn HistGradientBoostingClassifier（LightGBM 同等・libomp 不要に変更）
- 時系列分割（test >= 2025-07-07）: *ROC-AUC 0.880*（M2 単独ベスト 0.780）/ *PR-AUC 0.259*（無情報 0.021 → 約 12.4 倍）
- 次週リスク Top: 秋田市周辺に集中（最高 ~98%）
- 時間帯分布: 日中(10-16) 最多 → *通報バイアスの可能性*を UI に明記
- 可視化: web/index.html を「次週リスク / 出没実績」2 モードのダークダッシュボードに刷新（逐次配色 YlOrRd・市町村横棒ランキング・時間帯チャート・#1 パルス）

## M2 実データ検証結果（2026-06-05, live CKAN）

- パネル *1,739,784 行* = 8,168 メッシュ × 213 週（2022-04-04 〜 2026-04-27）
- 正例率 0.80%（テスト期間 2.09%）。範囲を「出没メッシュ + 8 近傍」に絞り現実的な不均衡に
- 時系列分割（train < 2025-07-07 / test 以降）の単独特徴ベースライン:
  | スコア | ROC-AUC | PR-AUC |
  |---|---|---|
  | nbr8_lag1（近傍8前週） | 0.753 | *0.168* |
  | weeks_since_last | *0.780* | 0.140 |
  | roll4_count | 0.715 | 0.152 |
  | lag1_count | 0.627 | 0.130 |
- *示唆*: 空間自己相関（近傍）が PR-AUC 最高 / 直近性が ROC 最高。HANDOFF の設計仮説を実データが支持。LightGBM で束ねれば伸びしろあり

## M1 実データ検証結果（2026-06-05, live CKAN）

- 生レコード *21,529 件* → クレンジング後（ツキノワグマ・座標/日時有効）*20,338 件*
- 期間: *2010-06-29 〜 2026-04-30*
- 市民投稿（`【投稿】`）: 6,339 件（31.2%）→ 信頼度特徴量として保持
- 市町村 Top3: 秋田市 5,772 / 大館市 2,081 / 大仙市 1,699
- 情報種別: 目撃 19,252 / 痕跡(その他) 498 / 痕跡(食害) 440 / 人身被害 148

## ローカルでの materialize（1 回だけ実行）

```bash
cd ~/projects/kuma-prediction
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python -m src.build_db        # data/kumadas.db + web/points.js を生成
open web/index.html           # 地図で出没点を確認
```

## 次アクション（M4 / 精度・運用の改善候補）

1. *外部特徴量*: 堅果類豊凶指数（年変動の主因）・土地被覆エッジ・標高/傾斜・気象 → 精度改善
2. *モデル比較*: lightgbm（要 `brew install libomp`）と HGB の比較。確率較正（CalibratedClassifierCV）
3. *時系列 CV の多分割*: 単一 holdout → ローリング forward-chaining で安定性評価
4. *バッファメッシュの市町村ラベル*: 最近傍の出没メッシュから市町村を補完（現状「（周辺）」）
5. *時間帯の通報バイアス補正*: 目撃 vs 痕跡/人身被害で時刻分布を分けて提示
6. *月次更新の自動化*: resource_id 追従 + build_db → build_panel → train を 1 コマンド化

## 設計の参照

- 全体方針: `HANDOFF_kuma_prediction.md`
- 設定（月次更新追従）: `src/config.py` の `RESOURCE_ID`
